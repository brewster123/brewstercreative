Place your custom font files here:
- PrimeformProDemo-Regular.otf (or .ttf, .woff2)
- PrimeformProDemo-Bold.otf (or .ttf, .woff2)
- Primeform Pro Demo.otf

The application is configured to automatically load "Primeform Pro Demo" from:
1. Local system fonts (if installed on your OS)
2. This /public/fonts directory
3. In-browser FontFace API or dynamic file loader
